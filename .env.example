# Path to skills/ directory (absolute or relative to project root).
SKILLS_ROOT=./skills

# HTTP port the MCP server + Web viewer listens on.
PORT=8080

# Reverse-proxy prefix. Behind nginx /mcp/, set to /mcp. Empty for local dev.
BASE_URL_PREFIX=

# Log level: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO
