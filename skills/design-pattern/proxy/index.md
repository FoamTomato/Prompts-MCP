---
name: design-pattern-proxy-index
description: 代理模式 — JDK 动态代理与 CGLIB 的区别及 Spring AOP 的选用规则。Use when 写动态代理 / 排查 AOP 没生效 / 纠结代理基于接口还是子类时。
parent: ../index.md
children:
  - { name: proxy-jdk-vs-cglib, path: jdk-vs-cglib.md, tag: skill, note: "JDK 动态代理 vs CGLIB，Spring AOP 何时切换" }
when_to_descend: 写动态代理、排查 AOP 未生效、纠结代理实现时
---

# Proxy · 子项索引

| 你在做什么 | 进哪个 |
|-----------|-------|
| 写动态代理 / 排查 Spring AOP 没生效 / 纠结代理基于接口还是子类 | [jdk-vs-cglib](jdk-vs-cglib.md) |
